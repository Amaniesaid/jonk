# ============================================
# DEV profile - SonarQube (temporaire)
# ============================================
# Objectif: rendre l'étape SonarQube fonctionnelle en environnement de dev
# sans demander à l'utilisateur final sonar.host.url / sonar.projectKey.
#
# ⚠️ En PROD: remplacer `token` par un Secret Manager / variable d'env.

jonk:
  sonarqube:
    host-url: "http://host.docker.internal:9000"
    token: "squ_83f76ad0fb3ff1dd13cd5e6ec038b49432fc9909"
    poll-interval: 2s
    quality-gate-timeout: 5m
    docker-compose-path: "./docker-dev-env/docker-compose.yml"

  keycloak:
    server-url: http://host.docker.internal:8180
    realm: Jonk
    client-id: jonk-back
    client-secret: jonk-secret
